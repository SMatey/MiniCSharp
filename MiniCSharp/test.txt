// test.txt - Versión Verificada y Limpia
class IntegrationTestProgram
{
    // --- Declaraciones Globales ---
    int g_int;
    double g_double;
    string g_str;

    class Point {
        int x;
        int y;
    }

    Point g_point;

    // --- Declaraciones de Métodos ---
    void SetValues(int val, string name)
    {
        g_int = val;
        g_str = name;
    }

    int GetIntValue()
    {
    }

    // --- Método Principal de Prueba ---
    void Main() 
    {
        int local_int;
        Point local_point;

        // --- Pruebas Válidas ---
        local_int = 10 * 2 + 5;
        g_double = local_int * 3.14;
        SetValues(100, "Test Name");
        GetIntValue();
        local_int = GetIntValue();
        local_point = new Point();
        g_point = null;
        local_int++;
    }
}